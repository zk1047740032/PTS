## 重构硬编码IP地址为动态配置

### 修改文件
`test/Rin_FSV3004.py`

### 修改内容

**1. RinAnalyzer 类（约60行）**
- `__init__`: 添加 `self.ip_address` 属性
- `connect()`: 保存IP地址到实例变量
- `measure_segment()`: 使用 `self.ip_address` 替代硬编码（第143行）

**2. BackgroundNoiseAnalyzer 类（约60行）**
- `__init__`: 添加 `self.ip_address` 属性  
- `connect()`: 保存IP地址到实例变量
- `measure_and_screenshot()`: 使用 `self.ip_address` 替代硬编码（第595行）

**3. TestRunner 类（约80行）**
- `run_rin()`: 从GUI获取IP，传递给RinAnalyzer，清理时使用传入IP（第734行）
- `run_background()`: 从GUI获取IP，传递给BackgroundNoiseAnalyzer

**4. TestGUI 类（约20行）**
- `start_rin()`: 创建RinAnalyzer时传入IP地址
- `start_background()` / `start_seedlight()`: 创建BackgroundNoiseAnalyzer时传入IP地址

### 预期效果
- 用户在GUI输入的IP地址将被所有功能使用
- 不再有硬编码的"192.168.7.10"
- 保持向后兼容（GUI默认值仍为"192.168.7.10"）