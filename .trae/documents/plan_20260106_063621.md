## 实现步骤

1. **修改 `_ensure_process_data_dir` 方法**：
   - 添加 `test_group` 参数
   - 根据测试组号返回不同的文件夹路径（"过程数据1"或"过程数据2"）
   - 更新方法文档字符串

2. **修改 `_save_screenshot` 方法**：
   - 将 `test_group` 参数传递给 `_ensure_process_data_dir` 方法
   - 更新方法文档字符串

3. **修改 `_save_all_curves` 方法**：
   - 将 `test_group` 参数传递给 `_ensure_process_data_dir` 方法
   - 更新方法文档字符串

## 具体实现细节

1. **文件夹命名逻辑**：
   - 当 `test_group` 为 1 时，使用 "过程数据1"
   - 当 `test_group` 为 2 时，使用 "过程数据2"
   - 保持其他逻辑不变

2. **参数传递**：
   - 在 `_save_screenshot` 和 `_save_all_curves` 方法中，将接收到的 `test_group` 参数传递给 `_ensure_process_data_dir` 方法

3. **文档更新**：
   - 更新所有相关方法的文档字符串，反映新的文件夹命名规则

## 注意事项

1. 保持原有代码结构不变，只修改文件夹命名逻辑
2. 确保所有调用 `_ensure_process_data_dir` 方法的地方都传递了 `test_group` 参数
3. 添加适当的日志记录，便于调试和监控
4. 处理可能的异常情况，确保测试不会因文件夹创建失败而中断