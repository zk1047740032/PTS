## 实现方案

1. **功能需求**：将中心频率改为可变参数，添加到UI界面的第一组测试和第二组测试中，默认值为80MHz

2. **实现步骤**：

   ### 步骤1：在params字典中添加中心频率参数

   * 文件：`d:\Coding\Project\PreciTestSystem\PTS\qijian\CT_L.py`

   * 位置：第1050行的`self.params`字典中

   * 添加两个新参数：

     * `group1_center_MHz`: 第一组测试的中心频率，默认值180.0

     * `group2_center_MHz`: 第二组测试的中心频率，默认值180.0

   ### 步骤2：在UI界面添加中心频率输入框

   * **第一组测试**：在`group1_frame`中添加中心频率参数输入框

   * **第二组测试**：在`group2_frame`中添加中心频率参数输入框

   * 使用现有的`_add_param_entry`方法进行添加

   * 添加位置：分别在两组测试的其他参数下方

   ### 步骤3：修改测量线宽的代码

   * 文件：`d:\Coding\Project\PreciTestSystem\PTS\qijian\CT_L.py`

   * 位置：第202行和第266行的硬编码中心频率设置

   * 修改为使用相应的中心频率参数

3. **具体代码修改**：

   ### 修改1：添加params参数

   ```python
   self.params = {
       # 现有参数保持不变
       "group1_center_MHz": 180.0,
       "group2_center_MHz": 180.0,
   }
   ```

   ### 修改2：添加UI输入框

   * **第一组测试**（第1151行后）：

     ```python
     self._add_param_entry(group1_frame, "group1_center_MHz", "中心频率(MHz):", self.params.get("group1_center_MHz", 180.0), row=8)
     ```

   * **第二组测试**（第1173行后）：

     ```python
     self._add_param_entry(group2_frame, "group2_center_MHz", "中心频率(MHz):", self.params.get("group2_center_MHz", 180.0), row=6)
     ```

   ### 修改3：修改测量线宽代码

   * 找到使用中心频率的位置，将硬编码的80MHz替换为相应的参数

   * 确保测试过程中正确获取UI输入的中心频率值

4. **预期效果**：

   * UI界面的第一组测试和第二组测试中都将显示中心频率输入框

   * 默认值为180MHz

   * 用户可以根据需要修改中心频率

   * 测量线宽时将使用用户设置的中心频率

5. **技术要点**：

   * 使用现有的参数管理机制，保持代码一致性

   * 为两组测试分别设置独立的中心频率参数

   * 确保参数值正确传递到测量线宽的函数中

   * 保持UI布局的美观和一致性

