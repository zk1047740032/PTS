## v5.3.0-2026.7.21

### 新增
- **系统-网络配置**：新增网络配置工具（`utils/network_config.py`），支持自动检测仪器子网、一键添加辅助 IP（netsh），省去手动配静态 IP 的操作；主界面"清空日志"按钮替换为"网络配置"入口；

### 修改
- **系统-VISA**：1.VISA 后端改用 `pyvisa-py`（纯 Python，免装 NI-VISA），`@py` 优先、默认后端兜底；2.TCPIP 协议从 HiSlip 切为 VXI-11（`TCPIP0` → `TCPIP`），提高 pyvisa-py 兼容性；3.打包配置同步加入 `pyvisa_py` 及子模块 hidden import；


## v5.2.0-2026.7.20

### 修改
- **系统-架构**：1.重构 `main_platform.py` 模块导入与初始化逻辑，新增模块注册表替代硬编码导入分支，便于后续扩展；2.光开关连接改为延迟初始化，一键测试时再建立连接，避免启动时阻塞；3.修复二级标签页初始化逻辑，统一管理 `sub_nb` 实例；
- **系统-配置**：1.引入全局配置 `CFG` 单例（`core/config.py`），将项目内所有硬编码的仪器地址、文件路径、时序参数、测量默认值等统一迁移至 `CFG`，实现配置集中化管理；2.新增配置参数面板模块（`utils/config_params_panel.py`），支持可视化编辑各项配置参数并持久化到本地 JSON 文件，主程序启动时自动加载用户配置；
- **系统-光路**：调整光路分组与通道编排逻辑，优化测试流程；
- **系统-UI**：1.新增 `utils/theme.py` 主题模块，统一管理颜色、字体、按钮样式等视觉元素，整体美化主界面及各弹窗风格；2.优化测试结果弹窗关闭按钮的样式和配色；3.取消 `messagebox.showerror` 弹窗，错误信息改为日志区输出，减少弹窗打断操作流程；

### 新增
- **系统-测试结果弹窗**：新增测试结果汇总弹窗（`utils/test_result_dialog.py`），一键测试完成后自动弹出，汇总展示各通道测试项的执行状态（含单频检测结果），便于快速确认测试完整性；
- **系统-报告**：1.报告数据组装中加入单频状态字段，根据对应输出目录是否有文件自动判断单频测试是否存在；2.报告支持写入种子型号、序号、序列号等 DFB 种子信息；

## v5.1.1-2026.7.17
### 修改
- **系统-架构**：完成已有各测试项的初步抽象，提取公共基类与工具至 `core/` 目录，`path_a/`、`path_b/` 全部 9 个程序迁移完成：
  - **core 基类**：`VisaInstrument`（VISA 仪器连接/读写/截图/关闭的通用控制器）、`BaseTestGUI`（tkinter 窗口/日志/按钮/自动关窗的通用界面）、`BaseTestRunner`（测试线程编排基类）；
  - **core 工具**：`read_instrument_screenshot()`（SCPI 截图读回）、`write_xy_csv()`/`append_row_csv()`（CSV 落盘）、`clear_directory()`（目录清理）、`visa_address()`（地址构造）；
  - **已迁移程序**：`Rin_FSV3004`、`Power`、`PhaseNoise`、`LightSwitch`、`LineWidth_FSV3004`、`SingleFrequency`、`SpectrumSNR`、`TimeDomain`、`WaveLength`；
  - **迁移原则**：零行为变更（不改测试逻辑/数值/日志文案）、向后兼容优先（参数与钩子吸收存量差异，保留 `self.instrument` 等业务别名）、不强制套模板（`TestRunner`/复杂多线程编排按需保留原结构）；

## v5.1.0-2026.7.13

### 修改

- **系统-一键测试**：优化报告，可写入更多数据；

### 新增
- **系统-DFB数据**：新增DFB数据读取模块，可读取种子信息；


## v5.0.0-2026.6.29

### 修改

- **系统-光开关**：1.封装 OpticalSwitch 类（`path_a/LightSwitch.py`），通过 USB VISA 协议控制光开关，支持 `:OSW1:CHAN?` 查询与 `:OSW1:CHAN n` 切换，写入后自动回读验证；2.连接失败不阻塞主流程，切换异常有容错降级；

- **系统-一键测试**：1.重构 `run_selected_tests()`，按 MODULE_MAP group 将勾选项分为 ch1/ch2/ch3/光路B 四组；2.新增 `_orchestrate_channel_tests()` 后台线程，按 ch1→ch2→ch3 顺序编排：切换光开关→延时稳定→并行启动通道内模块→轮询等待全部完成→下一通道；3.光路B 无开关依赖，与光路A 并行执行；
- **系统-进程管理**：1.`trigger_test()` 测试完成后自动 destroy 窗口（成功 500ms/失败 1000ms），触发 mainloop 退出→进程终止，解决编排线程永久阻塞问题；2.`_wait_for_modules()` 改用 `dict.get()` 原子读取，修复主线程并发清理 `self.processes` 时的 TOCTOU 竞态条件；

## v4.0.4-2026.6.18

### 修改

- **Rin_FSV3004**：1.连接方式从 SOCKET 改为 VXI-11（inst0::INSTR），统一测量与数据传输使用同一连接；2.数据获取从 MMEM:STOR+MMEM:COPY（SMB）改为纯 SCPI 直读（TRACe:DATA? 读 Trace、MMEM:DATA? 读截图），彻底移除对 SMB 共享文件夹的依赖，新仪器无需再配置 SMB 凭据和网络共享；3.移除网络同步等待与仪器端文件夹清理逻辑，简化流程；
- **LineWidth_FSV3004**：1.截图与 Trace 数据改为纯 SCPI 直读，移除 MMEM:COPY 及 SMB 依赖；2.添加 save_data 超时自动重连重试机制；3.NdBdown 读取改为短超时（5s）+ 失败后清理 VISA 缓存，防止污染后续操作；4.超时时间从 10s 调整为 60s；

## v4.0.3-2026.6.5

### 修改

- **Rin_FSV3004**：兼容逗号作为小数分隔符的CSV数据，解决不同固件版本下CSV文件无法正确解析数值的问题

## v4.0.2-2026.5.21

### 修改

- **线宽**：在每次保存截图前，显式写入 HCOPy:FILE:NAME:AUTO:STATe OFF 和 HCOPy:DEVice:LANGuage PNG ，确保按程序指定的 .png 文件名保存；

## v4.0.1-2026.4.09

### 添加

- **相噪**：添加相噪测试模块，放在通道二下；

### 修改

- **线宽**：移动算式位置，优化显示效果；

## Rin\_4051\_new-2026-04.01

1.修改频率、范围；2.添加驰誉振荡峰检测机制；

## v4.0.0-2026.3.27

### 修改

- **系统**：1.重构系统结构，各部门独立。本系统拆分为种子部门独立的一体化测试系统；2.移除Rin\_4051，作为独立程序；3.单频程序移动到光路A-通道2下，PZT调制移动到光路B下；
- **报告**：将成功生成报告的日志改为超链接，双击可直接打开；

## v3.2.1-2026-3.24

### 修改

- **种子-PZT调制**：1.调整给信号与点击reset的先后顺序，解决曲线突变；2.10v测试后自动计算PZT调制范围并记录；
- **种子-报告**：添加PZT调制部分数据

## v3.2.0-2026-3.20

### 添加

- **种子-PZT调制**：添加PZT调制程序，暂时放在通道一；

## v3.1.0-2026-3.16

### 添加

- **系统**：1.添加一键生成报告功能；2.添加光开关切换功能；3.添加Power模块到zhongzi-通道1；
- **种子-报告**：1.Rin图和RMS积分；2.信噪比图与SNR值；3.线宽图与线宽值；4.功率值与RMS值；
- **种子-功率**：1.添加功率数据读取程序；2.添加烤机数据计算功能（RMS和P2P指标计算）；

### 修改

- **种子-Rin\_FSV3004**：1.添加记录RMS积分值的文件；2.增加图二最大值保存功能；
- **种子-线宽**：1.添加记录每组测试Ndbdown值的文件；2.测试图中添加算式"NdBdown值 / (2√99) ≈ 结果"；
- **种子-信噪比**：增加数据保存功能；
- **种子-单频**：默认峰值阈值改为3db
- **种子-报告**：1.完成Rin的图片与数据的写入；2.更新报告模板中的功率稳定性显示；
- **界面优化**：为所有按钮添加hand2光标样式；

## v3.0.9-2026-3.5

### 修改

- **种子-Rin\_FSV3004**：将驰誉振荡峰检测范围右边界改为3e6；
- **种子-线宽**：信号发生器的连接添加重试机制，解决断连问题；
- **种子-信噪比**：1.修复SNR计算问题；2.开始测试前清空本地文件夹；

## v3.0.8-2026-1.30

### 修改

- **器件—CT\_W**：温度步进结束后自动循环，可设置循环次数；

## v3.0.7-2026.1.22

### 修改

- **种子-时域**：提供示波器与信号发生器通道切换功能；

## v3.0.6-2026.1.17

### 修改

- **种子-Rin\_FSV3004**：修复数据读取和处理的边界条件问题

## v3.0.5-2026.1.7

### 修改

- **种子-Rin\_FSV3004**：1.添加指令：Trace->Trace1->Mode: Average, Detector Type: RMS；2.添加指令：Amplitude->Input Coupling: DC，Reference Level->Unit: V；
- **种子-时域**：增加适配仪器，白色示波器（RIGOL-DS1104）；
- **器件-CT\_W**：保存每次步进下的曲线数据和曲线截图；

## v3.0.4-2025.12.25

### 修改

- **系统**：优化代码结构，去除弃用函数；
- **器件-CT\_L**：将中心频率改为可变参数，短波需要在180MHZ下测试；
- **种子-线宽**：1.添加设置y轴单位为v的指令；2.将Ref level设置为可变参数；3.关闭Marker1标记自动搜索峰值，改为手动设置标记位置，以应对50km峰值失真问题；

## v3.0.3-2025.12.19

### 修改

- **种子-单频**：1.\*OPC? 阻塞等待确保仪器完成扫频，增加临时超时设置和异常处理，防止同步失败；2.移除未使用的扫描相关代码，简化代码结构；3.去除弹窗，避免弹窗过多影响使用；4.添加定时功能；5.添加电流、温度、扫频循环次数统计，添加检测出峰次数统计，并实时显示；6.开始时先清空文件夹；

## v3.0.2-2025.12.15

### 修改

- **种子-单频**：1.将频谱仪纵轴单位从V改为DBM，并简化数据处理逻辑，直接使用频谱仪返回的DBM值；2.调整1.5μm默认参数；

## v3.0.1-2025.12.13

### 修改

- **系统**：1.回滚系统架构至中央控制平台➕独立小窗模式以解决多线程卡顿问题，依然支持一键测试；

## v3.0.0-2025.12.11

### 修改

- **系统**：1.所有程序模块集成到统一窗口内，实现一键测试功能；2.在系统中内置简要说明文档，方便生产人员上手使用以及排查问题；
- **种子-线宽**：1.信号发生器频率单位从KHZ改为HZ；2.Ref Level设置为-20dBm，优化1M、2M时的显示效果；
- **种子-测Rin**：优化驰誉振荡峰检测算法；

## v2.3.7-2025.12.05

### 修改：

- **系统**：1.对程序架构进行了系统性修改，引入了多进程架构模式；2.添加进程监测窗口，实时监控进程运行状态；
- **种子-时域**：1.当时基模式为ROLL模式时，水平时基自动变为50ms，图像在x轴上过于密集，显示效果差，因此添加设置时基模式为YT模式的指令；2.在截图弹窗title中添加频率信息，便于区分；3.不再自动保存曲线数据文件；
- **种子-线宽**：在测线宽前若使用测Rin程序测底噪，则AverageCount会设置为1，因此在线宽程序中添加将AverageCount设置为20的指令;

## v2.3.6-2025.12.04

### 修改：

- **种子-线宽**：最后一组测试，信号发生器从三角波改为正弦波；
- 系统-修改各程序默认IP地址，为一体化系统组网做准备；

## v2.3.5-2025.12.03

### 修改：

- **种子-单频**：1.新流程：温度电流独立循环，持续细扫检测；
- **种子-时域**：1.在100Hz测试结束后自动进行300Hz测试；100Hz的时基设为10ms,300Hz的时基设为2ms；2.测试结束后，向示波器发送STOP指令，并终止信号发生器的输出；

## v2.3.4-2025.11.28

### 修改：

- **种子-信噪比**：1.固定图像显示区域，以显示完整曲线（Ref Level默认设置为-4.0dBm，可调整）；
- **种子-时域**：1.根据种子的稳定峰峰值智能选择示波器垂直刻度放大倍数，以在对不同种子测试时都能达到最佳显示效果；

## v2.3.3-2025.11.27

### 修改：

- **种子-线宽**：1.原测试流程结束后，额外控制信号发生器+1v电流进行一组500KHz下的测试，并保存截图和数据；

## v2.3.2-2025.11.26

### 修改：

- **种子-线宽**：1.提供与其他程序一致的UI界面，支持修改参数与查看运行日志；2.根据新需求，在Span为100KHz，200KHz，500KHz，1MHz，2MHz都保存截图，并在测试结束后提供预览窗口以便选图保存；

## v2.3.1-2025.11.18

### 修改：

- **种子-单频**：适配4051

## v2.3.0-2025-11-13

### 新增：

- **器件-电流温度\_线宽**；

### 修改：

- **种子-单频\_4052**：1.1.5μm模块变温保持粗扫；2.1μm模块，测试完后设置回细扫电流（初始电流）；

## v2.2.11-2025-11.10

### 修改：

- **种子-单频\_4052**：1.5μm模块按生产人员新需求重构扫描逻辑：不再电流巡检，而是给定粗扫1-细扫-粗扫2-粗扫3四个电流设置框，直接在对应电流下执行对应扫描逻辑；

## v2.2.10-2025.11.05

### 修改：

- **种子-Rin\_FSV3004**：测Rin后也自动保存一份图片
- **种子-单频\_4052**：按生产人员新需求重构扫描逻辑：开始时设置电流为电流上限，按步进递减，每一电流下判断是否为细扫电流（新参数），若为细扫电流，执行细扫流程，否则执行粗扫流程；

## v2.2.9-2025.11.03

### 修改：

- **种子-Rin\_FSV3004**：1.修复了FSV4测底噪与种子光只有图片没有数据的问题；2.添加一键修改文件名功能；
- **种子-光谱信噪比**：固定图像显示区域（Reference Level设置为-4.0dBm）
- 种子-单频\_4052：1.检测到异常峰后截图保存，不终止运行；2.统一每一电流下的扫描顺序；3.添加电流上限参数，在\[电流上限, 电流下限]内，初始电流细扫，其余电流下粗扫；4.添加暂停功能；

## v2.2.8-2025.10.29

### 修改；

- **种子-光谱信噪比**：自动设置灵敏度为HIGH1

## v2.2.7-2025.10.28

### 修改：

- **种子-Rin\_FSV3004**：1.测Rin添加驰誉振荡峰自动显示；2.尝试修复FSV4底噪与种子光不保存数据文件的问题；

## v2.2.6-2025.10.21

### 修改：

- **种子-单频\_4052**：修复了已知问题；

## v2.2.5-2025.10.18

### 修改：

- **种子-线宽**：修复路径错误导致的出图异常问题；
- **种子-Rin\_FSV3004**：1.底噪测试以dat格式保存对应数据；2.优化文件命名逻辑；3.添加种子光按钮；

## v2.2.4-2025.10.17

### 修改：

- **系统**-将文件夹名种子、器件改为zhongzi、qijian，避免中文报错；
- **种子-Rin\_FSV3004**：1.优化数据读取逻辑；2.优化底噪测试截图弹窗逻辑；3.优化前端界面；4.增加运行日志区；
- **器件-电流温度\_功率**：去除usb资源栏操作说明；

## v2.2.3-2025.10.14

### 修改：

- **系统**-允许多窗口打开，支持最小化；

## v2.2.2-2025.10.14

### 修改：

- **器件-电流温度\_波长**：将输出日志框布置在右侧；
- **器件-电流温度\_功率**：将输出日志框布置在右侧；

## v2.2.1-2025.10.14

### 修改：

- **器件-电流温度\_波长**：第一组测试不设置初始电流；
- **器件-电流温度\_功率**：第一组测试不设置初始电流；

## v2.2.0-2025.10.13

### 新增：

- **器件-电流温度\_功率**；

